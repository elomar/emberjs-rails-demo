<h2>Quick Facts</h2>

<table>
  <tbody>
    {{#each controller}}
      <tr>
        <th>{{name}}</th>
        <td>{{value}}</td>
      </tr>
    {{/each}}
  </tbody>
</table>

{{#if Auth.currentUser.admin}}
  {{render 'facts.new'}}
{{/if}}
